# Campaign-Start Preflight Report

## Repository identity

- Assigned repository: `westkitty/Heliocide_Viewer_dexgpt`
- Assigned branch: `main`
- Independent source publication: `11cb7cf168af44c8d4f27b85d8cddf6d22140ceb`
- Other Heliocide repositories: out of scope under the latest user correction.

## Independent implementation

The local baseline was authored from the controlling specification and the two canonical Shard God files retrieved from the user's connected Drive. It was not seeded from another Heliocide repository.

## Deterministic build gates

- `npm ci`: PASS
- `npm run typecheck`: PASS
- `npm test`: PASS (4/4)
- `npm run build`: PASS

## Browser evidence

The available browser policy blocks ordinary localhost/file navigation. The validation harness therefore injects the exact built document/source into fresh Chromium CDP tabs and records the real WebGL2/DOM runtime at 1600×900. Local campaign-start A/C/D/E/F/G/H captures completed with zero captured console/runtime errors.

Visible protected-path checks include the inhabited system, three crew, canonical dossier, stellar collapse, dark compact-object state, breach/decompression cues, starless Siege Wall, exterior station-loss shot, and forensic replay interface.

The final local interaction resweep passed 8/8 observable checks: replay unlock, camera/dossier switching, deterministic scrub, tactical console open/close, keyboard movement, restart/relock, and audio control. Trusted Pointer Lock remains environment-unverified because CDP synthetic input cannot prove a normal trusted user gesture.

## Health

Fresh project-health audit: **94/100**, no High/Critical findings. Remaining Medium finding: the environment-constrained direct WebGL2 renderer is not a confirmed Three.js package shape. Listener/disposal hygiene finding was repaired and the audit rerun.

## Performance evidence limit

No hardware GPU is exposed (`/dev/dri` absent); Chromium validation uses SwiftShader. Software-render frame measurements are useful for relative regressions and runtime-error detection but are not proof of target hardware performance.

## Repaired preflight defects

1. protected experience elements incorrectly hidden behind future visual gates;
2. renderer listener cleanup;
3. audio teardown using a stale field name;
4. player-visible obsolete labels baked into the canonical Shard God source plate, fixed nondestructively by runtime masks while preserving source bytes.

## Publication blocker

The assigned-repository connector has no local-file upload argument. A target-only Git-object identity probe was rejected without updating `main`. Direct numeric-IP network tests also fail, proving this is not merely DNS. The two canonical image binaries and required PNG evidence remain local-only; therefore the remote source commit is not being claimed as a complete runnable/evidenced baseline and no numbered iteration has been accepted.
